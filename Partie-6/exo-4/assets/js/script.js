let days = ['Lundi', 'Mardi', 'Mercredi', 'Jeudi', 'Vendredi', 'Samedi','Dimanche'];

let span = document.createElement("span");
    span.innerHTML = days;
    document.body.appendChild(span);

    days.forEach(days=>(days)); 

    if (days = 'Samedi') {
        span.style.fontWeight='bold';
      }
    